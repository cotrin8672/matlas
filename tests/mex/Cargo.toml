[package]
name = "rustmat-integration"
version = "0.0.0"
edition = "2021"
publish = false

[lib]
crate-type = ["cdylib"]

[dependencies]
rustmat = { path = "../.." }
rustmex = { version = "0.6.4", default-features = false, features = ["matlab800", "alloc"] }
